@*@model IEnumerable<TH05.Models.Product>*@

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using TH05.Models;
@using PagedList.Mvc;
@model PagedList.PagedList<Product>

<h2 style="text-align:center; color:green; padding-bottom:20px" >Danh sách sản phẩm</h2>
<div class="container">
    <div class="row">
        @foreach(var pro in Model)
            {
            <div class="col-sm-12 col-sm-6 col-md-4 col-lg-3" >
                <div class="card-pro">
                    <p>@Html.DisplayFor(model=>pro.ProductID)</p>
                    <img src="@Url.Content(pro.ImagePro)"height="150" width="150"/>
                    <p>@Html.DisplayFor(model=>pro.NamePro)</p>
                    <p>@Html.DisplayFor(model=>pro.Price)</p>
                    <a href="/ShoppingCart/AddToCart/@pro.ProductID" class="btn btn-default">Buy</a>
                </div>
            </div>
            }
    </div>
</div>
Trang @(Model.PageCount<Model.PageNumber?0:Model.PageNumber )/@Model.PageCount
<div class="MenuPage">
    @Html.PagedListPager(Model,page=>Url.Action("Index",new {page=page.ToString()}))
</div>

